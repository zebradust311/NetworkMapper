# enterprise benchmark

This dataset represents an enterprise segment with mixed infrastructure and server naming patterns.

Files:
- inventory.json: Input inventory used for classification benchmarking.
- expected_results.json: Expected DeviceType per IP address.