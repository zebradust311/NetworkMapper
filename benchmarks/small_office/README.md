# small_office benchmark

This dataset represents a compact office network with common endpoint and infrastructure device patterns.

Files:
- inventory.json: Input inventory used for classification benchmarking.
- expected_results.json: Expected DeviceType per IP address.